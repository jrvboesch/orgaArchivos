#ifndef API_H
#define API_H
#include "archivo.h"
#include "discovirtual.h"

using namespace std;
class API
{
public:
    API();
    /*string getName();
    string getPath();
    int getType();
    void agregar(Archivo * nuevo, Folder * donde);
    Archivo * next;
    Archivo * anterior;


    Archivo * crearArchivo(Folder * donde,string name,string type);
    string * listarArchivos(Folder *donde);
    Archivo * cargarArchivo(string path, Folder* donde);
    int eliminarArchivo(string path, Folder* donde);
    void copiar(string origen, string nombreArchivo,string destino);
*/

    //Archivo * buscar(string name, Folder* donde);

};

#endif // API_H
